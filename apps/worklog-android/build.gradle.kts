// Plugins are declared in the modules that use them. Declaring the Android
// plugins here (even with `apply false`) would force the Android Gradle Plugin
// onto the classpath of every build, including SDK-less `:core:test` runs.
