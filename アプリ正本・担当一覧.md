# アプリ正本・担当一覧

Ajuworks が開発するアプリの「正本」（どこが本物のソースか）と担当範囲の一覧。
新しいアプリを追加したら、必ずこの表を更新すること。

最終更新: 2026-09-20

---

## 一覧

| # | アプリ名 | 種別 | 正本の場所 | package / 識別子 | 状態 |
|---|---|---|---|---|---|
| 1 | 人生資金シミュレーター | Web (Next.js) | このリポジトリ直下 (`src/`, `public/`) | — | 公開済み |
| 2 | WorkLog ― 今日何時間働いた？ | Android | `apps/worklog-android/` | `com.ajuworks.worklog` | 実装完了・ビルド待ち |

---

## 1. 人生資金シミュレーター (Phase 1)

| 項目 | 内容 |
|---|---|
| 正本 | `/src`, `/public`, `/supabase`（リポジトリ直下） |
| 技術 | Next.js 14 / TypeScript / Tailwind CSS / Recharts |
| テスト | Vitest (`npm test`) |
| バックエンド | Supabase（任意・無くても動作） |
| 固定費 | 0円 |
| ドキュメント | `README.md` |

---

## 2. WorkLog ― 今日何時間働いた？ (Phase 2)

| 項目 | 内容 |
|---|---|
| 正本 | `apps/worklog-android/` |
| package | `com.ajuworks.worklog` |
| version | versionName `1.0.0` / versionCode `1` |
| SDK | minSdk 26 / targetSdk 35 / compileSdk 35 |
| 技術 | Kotlin / Jetpack Compose / Material 3 / Room / DataStore / WorkManager / Glance |
| データ | 端末内のみ。サーバー送信なし。ログイン不要 |
| 固定費 | 0円 |
| 収益 | Google AdMob（バナー＋低頻度インタースティシャル） |
| ドキュメント | `apps/worklog-android/README.md` |
| プライバシーポリシー | `apps/worklog-android/docs/privacy-policy.html` |
| ストア素材 | `apps/worklog-android/store/` |

### モジュールの責務

| モジュール | 責務 | Android 依存 |
|---|---|---|
| `core/` | 勤務時間の計算・集計・検証・CSV生成 | なし（純粋 Kotlin/JVM） |
| `app/` | UI・永続化・通知・ウィジェット・広告 | あり |

**重要**: 勤務時間の計算ロジックを `app/` に書かないこと。
`core/` に置くことで、Android SDK やエミュレータなしに
`./gradlew :core:test` で検証できる状態を保つ。

### 触るときの注意

- 日跨ぎ勤務は「出勤した日」に計上する。この規則を変えると全集計が変わる。
- 日時は epoch millis + 打刻時点の UTC オフセットで保存する。文字列にしない。
- 常駐タイマーを追加しないこと。経過時間は描画時に計算する。
- 打刻操作（出勤・退勤・休憩）に広告を紐づけないこと。
- 署名鍵・AdMob 本番 ID をリポジトリにコミットしないこと
  （`keystore.properties` は git-ignore 済み）。
